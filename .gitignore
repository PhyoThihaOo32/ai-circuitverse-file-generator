# Dependencies
node_modules/
backend/node_modules/

# Environment variables — never commit secrets
backend/.env
.env

# Build artifacts / large simulator bundle (tracked separately)
# backend/simulator-dist/simulator.js  <- keep this, it's the pre-built bundle

# macOS metadata
.DS_Store
**/.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Runtime
*.pid
*.seed
*.pid.lock
